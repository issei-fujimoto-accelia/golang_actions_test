name: "custome action"

inputs:
  name:
    description: "sample"
    required: true

runs:
  using: "composite"
  steps:
    hello1:
    - name: "hello"
      run: echo "hello custome action!!!!!!"
      
    hello2:
    - name: "hello"
      run: echo ${{ inputs.name }}
      
