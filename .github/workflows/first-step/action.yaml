name: "custome action"

inputs:
  name:
    description: "sample"
    required: true

runs:
  using: "composite"
  steps:
    hello:
    - name: "hello"
      run: echo "hello custome action!!!!!!"
      
    hello:
    - name: "hello"
      run: echo ${{ inputs.name }}
      
